# AGENTS.md

本文件适用于整个仓库。后续代理在修改代码、数据、测试或打包配置前，必须先阅读并遵守本文件。

## 2026-09-09 UTC：当前实现与用户最新授权（优先于下面历史记录）

用户明确授权本次不足 24 小时例外同步一次。已完成且不得再次调用：`item_info_all`、`item_price_all` 各一次；快照时间 `2026-09-09T00:30:51Z`，1722 条目录、1353 条价格。普通“扑克牌-”15 种全部有价；“阿萨拉牌-”55 种全部缺价。扑克牌 Q 从 1512 更新至 1612。缺价不等于零元、不可出售或整个 API 失效。以后仍执行 24 小时保护和用户主动授权，无后台同步。

`tools/update_prices_once.py` 默认只读审计；实际执行要求显式参数，先备份 SQLite，再调用两次端点并记录不含凭证的摘要。`--authorized-within-day` 仅供已经取得例外授权的维护操作。当前备份在 AppData `backups/before-price-sync-20260909T003041Z.sqlite3`，报告在 `reports/price-sync-20260909T003041Z.json`。目录与价格使用既有分步事务，不能宣传为新实现的原子双接口同步。

网页默认全部已录入物资按整件/整堆总价升序，缺价最后，来源标签区分背包、地上、安全箱、身上/口袋/胸挂和未分区。移除可见“待核对”门槛与常驻纠错侧栏；点击物品才打开纠错对话框。内部 `confirmed` 仍记录人工来源，不伪造人工确认；`comparable` 默认接受机器识别。特殊耐久物仍显示目录参考口径，缺价、未知、未分区、仅有枪体价不参与替换。默认接受不代表识别已经正确。

`advisor.py` 使用 CP-SAT 求解不重复使用物品的一换一匹配，目标为参考总价差最大，求解上限 0.5 秒。每个地上物品、每个背包物品至多用一次，只检查占格数，保护锁定、身上装备、安全箱以及不完整整枪。最优状态仅指此简化匹配模型，不能称为二维布局或全局最高撤离收益。允许过期快照比较并提示，删除旧版逐项确认/过期确认门槛。建议必须保留形状、空位与不完整识别的限制。

用户明确授权接入 DeepSeek：`llm_settings.py` 保存白名单模型设置，`deepseek_provider.py` 只访问官方固定 HTTPS 地址，无重试、无重定向、禁用代理。网页模型窗口可选关闭/DeepSeek/本机 Ollama。DeepSeek 密钥保存在 AppData `web-review/deepseek-token.dpapi`，设置文件 `web-review/llm-settings.json` 仅含 provider/model；GET 设置永远不返回密钥。它与物价凭证相互独立。启用模型默认不勾选，云端分析另需明确允许发送本次文字；不发送图片、游戏 UID 或截图文件路径。

`tools/configure_deepseek.py` 通过不回显输入读取密钥（不能作为命令行参数），加密保存；`--test-once` 仅发送合成规则做一次联调。已完成一次真实测试，DeepSeek V4 Flash 返回有效 JSON 与引用，235 tokens。不再自动重复测试。RAG 为本地 BM25 检索规则/目录/当前物资与差价证据 → LangChain LCEL → 可选 LLM 解释，模型不改价格、不直接控制工具/游戏；引用存在不保证所有解释正确。

当前网页服务已重启，保留用户最新会话 35 项、revision 0，加载新价格；这张图的一换一建议为空是符合当前价格与筛选条件的结果，不伪造增益。99 个 Python 测试、8 个前端逻辑测试、Ruff 与 JS 语法检查通过；HTTP 路由和模型真实连通已检查，未做浏览器点击/截图验收。桌面 EXE 没有重新打包。新增模块、学习路线、使用步骤见 `docs/AI-ENGINEERING.md` 与 `docs/WEB-REVIEW.md`。

## 2026-09-08 增强识别、全部范围与 AI 工程工作流

新增全图多尺度 OCR（6 个重叠分块＋最多 16 个紧凑标签补读）、坐标反变换与空间去重。武器优先用可见弹药计数角标推断横竖方向；装备框尝试匹配可见边界，并限制小物品不能吸附到整个容器边框。未识别的有证据区域可显示未知框，不伪造物品 ID。仍有漏项和型号/数量错误，不声称准确率达标、任意角度鲁棒或已完成训练。

网页版增加 `all` 排名范围（只允许用于排名，不是物品所属位置）、来源标签、增强补扫和条件建议。全部包含地上物品，合计不是已携带收益。补扫保护已保存纠错、确认、锁定、手动和排除项，不自动删除旧候选。保留候选也可能重复，必须复核。

`advisor.py` 只做确定性候选比较：未知/未确认/配置不全不进入替换比较；锁定、安全箱不作为丢弃候选。差价由程序算，各条是独立备选，数量格数相容不等于二维可放置，不能累计执行或称为全局最优。

`knowledge.py` 是中文 bigram BM25 本地稀疏检索，知识来自项目规则、目录和快照。`agent_workflow.py` 使用可选 `langchain-core==1.6.2` 的 LCEL 编排检索→规则工具→解释，默认关闭 LLM。勾选后仅能访问固定本机 Ollama `127.0.0.1:11434/api/chat`，禁用代理、重定向、云追踪和重试，不下载模型、不发送截图或 Token。LLM 只解释证据，引用 ID 必须有效，失败保留规则结果。有效引用不代表内容一定正确；模型文字不改变价格和操作建议。

当前是可审计工作流，不是 LLM 自主规划 Agent；无本机模型时不能写成已完成真实 LLM 验收。学习路径、模块作用和官方资料见 `docs/AI-ENGINEERING.md`。桌面 EXE 未重打包，付费接口未调用。新增开发工具 `tools/evaluate_recognition.py` 只读评估原图；`tools/restart_web_preserving_session.py` 在内存保留会话等待维护重启，不终止进程、不保存截图。

验证：88 个 Python 测试、7 个前端逻辑测试通过；真实当前会话补扫后 45 项（含 4 个未知），保存的人工项和确认状态保留。原图增强识别约 30 秒，未达 2 秒指标，也未完成大规模准确率验收。当前本机地址仍为 `http://127.0.0.1:18765/`，已加载新后端；Windows 服务设置端口独占，避免两个实例争用同一地址。

## 2026-09-08 先显示参考价格更新

按用户最新要求，网页版先显示候选单价、总价、每格价和参考排序，不再等待全部人工确认。顶部合计含未确认候选，明确不代表完整背包价值；缺价显示未定价，排除项不计入。原有服务端已核对排名及完整性门槛保持不变，不能把前端候选排序称为可靠丢弃建议或全局最优。

价格表移到截图上方，原图编号附带参考总价；枪械未填整件总价时标注枪体参考价、不含完整配件。允许先保存未确认纠错。手动框选后自动调用已有本地四方向裁剪识别，用户选候选或搜索后保存即可更新参考价；没有实现漏项自动全补、隐藏配件识别或自动训练。

本次仅更新网页静态资源、说明和回归测试，不需重启现有服务或丢失当前截图；刷新页面前先保存正在编辑的字段。未调用付费接口。新增纯前端逻辑测试命令：`node --test tests/web_preview.test.cjs`。

## 2026-09-08 网页第一阶段更新

新增本机网页端，入口为外层 `启动网页版.cmd`，地址 `http://127.0.0.1:18765`。当前目标是截图核对与可信参考排名，不是全局理包。详细流程、局限和数据路径见 [WEB-REVIEW.md](<C:/Users/ASUS/Desktop/delta force/delta-loot-assistant/docs/WEB-REVIEW.md>)。

复用目录/价格/图标/OCR：原数据库只读、不读取 Token、不调用付费接口。纠错单独存于 AppData 的 `web-review/corrections.sqlite3`。完整截图仅在内存保留；裁剪保存必须用户勾选。同图纠错恢复仍需重新确认，不等于自动训练。

全图文字位置识别不依赖固定背包坐标，手动框选支持不同分辨率；裁剪重识别尝试四个正交方向。框大小/区域/型号可能误判，全部候选仍需确认。未知、缺价、未分区和整枪配置不完整阻止可信排名；枪械等特殊物品当前要求核实后的整件总价。详情图配件关联尚未实现。

网页使用 Python 标准库本机 HTTP 服务和静态前端，后台线程执行识别；没有新增外部依赖或云部署。不得未经授权迁移真实数据到网上。桌面 EXE 未随本轮网页修改重新构建。

本轮网页验证：65 项自动测试通过，Ruff 与 JS 语法检查通过；`02.png` 已真实走通上传/候选生成，裁剪四方向重识别可返回候选。结果仍需人工核对；未进行大规模准确率和完整浏览器点击验收。

## 2026-09-08 文件整合更新（路径以此节为准）

用户随后要求整合冗余文件并替换旧程序。桌面旧 `DeltaLootAssistant`、源码 `.venv-test`、`build`、`dist`、两个检查缓存及 `smoke.sqlite3` 已移入外层 `_archive/2026-09-08-cleanup`，没有永久删除。当前 `.venv`、源码和 AppData 数据保持原位置。

新版便携程序输出为 `C:\Users\ASUS\Desktop\delta force\app\DeltaLootAssistant`，统一入口为外层 `启动助手.cmd`。开发仍用本仓库 `start.cmd`。重打包不代表识别准确率或全局理包已完成。

本轮实际验证：37 项测试及 Ruff 通过；新 EXE 隔离冒烟退出码 0，覆盖界面、演示优化器及本地 OCR。真实数据库、图标索引和加密凭证大小/修改时间保持不变；未调用付费接口。构建记录见外层 `app/BUILD-INFO.md`。

`./build.ps1 -SkipInstaller` 默认离线使用本地依赖；显式 `-InstallDependencies` 才运行 pip。构建检查退出码，失败立即停止；缓存放外层 `_archive/build-cache`。Inno Setup 配置跟随新输出路径，本次不安装/卸载 Windows 程序。

打包入口的 `--smoke-test --smoke-report <路径>` 使用临时数据目录并禁用 socket 连接，验证窗口、演示优化器及本地 OCR 模型加载/空白图推理，不触碰真实数据或注册热键。它不替代真实截图和游戏热键验收。

下方详细手册及 `docs/file-inventory` 为**整理前历史状态快照**；旧路径按 [归档映射](<C:/Users/ASUS/Desktop/delta force/_archive/2026-09-08-cleanup/README.md>) 查找。“旧 EXE 未重打包”“保留桌面旧目录”不再代表本轮安排。当前操作见 [使用说明](<C:/Users/ASUS/Desktop/delta force/使用说明.md>)。付费接口、密钥与游戏只读约束继续有效。

本文同时是给项目所有者看的中文交接手册。**上半部分“项目目标”是最终方向，不代表全部已经实现；实际进度以文末“2026-09-08 实际状态与使用手册”为准。** 本次只整理说明和文件清单，没有再次请求付费接口，没有更新安装包。

## 项目目标

这是一个 Windows 端、只读的《三角洲行动》烽火地带最高收益理包助手。

- 用户主动按 `F8` 截图或追加截图，按 `F9` 完成扫描并求解。
- 程序识别自身装备、容器和敌方物资，并计算撤离总估价最高的合法布局。
- 输出提示、最终布局和编号操作步骤，不代替用户操作游戏。
- 优先完成真实物品数据库、价格快照和可验证的优化逻辑，再扩充截图识别数据集。

## 不可突破的安全边界

- 不注入游戏进程，不读取内存，不抓包，不分析或修改游戏网络流量。
- 不发送鼠标或键盘操作，不自动拖动物品，不实现宏、自动控制或反检测。
- 不覆盖游戏画面；只使用独立置顶侧边窗。
- 不识别战斗、敌人、地图或未由用户主动打开的容器。
- 不自动抓取 KK 一图流或其他禁止自动化访问的网站。
- 不绕过接口鉴权、限流、付费机制或服务条款，也不搜索、使用或提交泄露的密钥。

如果用户要求突破上述边界，应停止实现该部分并明确说明风险。

## 付费价格接口规则

三角洲数据帝是用户自行付费并授权使用的数据源。网络访问必须遵守以下规则：

- 只允许用户明确触发“手动日更”，禁止启动时同步、后台轮询、定时任务或静默重试。
- 一次完整快照最多请求一次 `item_info_all` 和一次 `item_price_all`。
- SQLite 中已有 24 小时内成功快照时，必须在发送任何请求前拒绝重复同步。
- 请求失败时不得自动重试；先向用户报告可能产生的资源消耗，获得明确许可后才能再次请求。
- 测试必须使用假客户端或固定夹具，绝不能访问真实接口。
- 同步成功后保留本地版本和时间戳；求解过程只读取本地 SQLite，不访问网络。
- 不把第三方页面宣传内容当成独立验证过的官方事实。

## Token 与敏感数据

- Token 不得写入源代码、测试、文档、命令示例、日志、异常消息、截图或 Git 历史。
- Windows 客户端使用当前用户的 DPAPI 加密保存 Token；环境变量仅用于临时开发和测试。
- 不输出带有 `token` 查询参数的完整 URL，也不输出完整接口响应。
- 不提交 `%LOCALAPPDATA%\DeltaLootAssistant`、SQLite 数据库、加密 Token 文件或真实价格快照。
- 如果 Token 曾出现在聊天、日志或其他可复制位置，提醒用户在联调后轮换。

## 当前技术栈与平台约束

- Python `3.12`，不应在未评估打包兼容性的情况下升级主版本。
- PySide6、OpenCV、ONNX Runtime、OR-Tools CP-SAT、SQLite。
- 目标环境为 Windows 11、1920×1080、简体中文、100% 游戏 UI 缩放、无边框或窗口模式。
- 使用 PyInstaller 构建程序，使用 Inno Setup 生成安装包。
- 默认 UTF-8；中文乱码通常是终端显示编码问题，不要据此机械重写文件。

## 代码结构

- `src/delta_loot_assistant/models.py`：公共领域模型与结果类型。
- `src/delta_loot_assistant/pricing.py`：本地价格包、SQLite 版本与估价。
- `src/delta_loot_assistant/orzice.py`：授权数据源客户端、目录快照和手动日更。
- `src/delta_loot_assistant/secrets.py`：Windows DPAPI Token 存储。
- `src/delta_loot_assistant/catalog.py`：物品目录加载与搜索。
- `src/delta_loot_assistant/recognition.py`：混合图像识别。
- `src/delta_loot_assistant/optimizer.py`：CP-SAT 最优解模型。
- `src/delta_loot_assistant/action_planner.py`：从当前状态到目标状态的步骤生成。
- `src/delta_loot_assistant/controller.py`：桌面流程编排。
- `src/delta_loot_assistant/ui/`：置顶侧边窗与校准界面。
- `resources/`：可提交的演示配置和样例，不得放真实 Token。
- `tests/`：单元、优化器、识别与控制器测试。

## 数据建模约定

- 当前本地物品主键为 `df:<objectID>:source:<oid 或旧记录 id>`，用于区分官方物品 ID 相同的供应商记录。不要未经迁移改回只有 `df:<objectID>`，否则可能覆盖变体记录。
- 数据帝基础物品的 `oid` 与价格记录的 `id` 关联；必须先验证关联成功再生成价格包。
- 整枪价值由枪体、已安装配件、弹匣和装填弹药组成，子项不得重复计价。
- 弹药按实际发数计价；不可出售物品的金钱价值为 `0`，但仍可锁定。
- 未从可靠数据源获得的堆叠、重量、兼容性、安全箱限制和耐久规则必须标记为暂定规则，不能伪装成已验证数据。
- 低置信度或未确认物品不得静默参与“全局最高收益”结论。
- 只有 CP-SAT 返回 `OPTIMAL` 时显示“最高收益”；`FEASIBLE` 必须显示尚未证明全局最优。

## 修改原则

- 保留用户已有修改；不要重置、覆盖或清理无关文件。
- 编辑文件使用 `apply_patch`，机械格式化除外。
- 优先进行小而可验证的修改，避免一次重写多个稳定模块。
- 新增外部依赖前，评估 Windows、Python 3.12 和 PyInstaller 打包影响。
- 网络、数据库迁移、求解约束和密钥处理的修改必须增加测试。
- 不把真实截图、图标、价格数据或第三方素材提交仓库，除非用户确认拥有分发授权。

## 验证命令

在仓库根目录运行：

```powershell
.\.venv\Scripts\python.exe -m ruff check src tests
.\.venv\Scripts\python.exe -m pytest
```

涉及打包时再运行：

```powershell
.\build.ps1
```

测试通过不代表接口可以重复调用。真实接口验证仍须单独取得用户许可，并遵守付费接口规则。

## 完成标准

- 所有相关测试和 Ruff 检查通过。
- 没有 Token、真实价格快照或用户数据进入 Git。
- 没有新增自动游戏操作、进程访问或后台网络行为。
- 数据来源、更新时间、暂定规则和求解状态在 UI 中表达准确。
- 向用户说明实际完成内容、验证结果、剩余数据缺口以及是否发生过付费接口请求。

---

## 2026-09-08 实际状态与使用手册

### 一、先看结论：现在能做什么

当前是“本地价格快照 + 截图候选识别 + 人工确认 + 单格价值排序”的可运行源码原型，不是已经验收的全自动识别器，也不是能对真实对局证明最高收益的完整理包系统。

- 已有真实目录、真实价格快照和下载到本机的物品图标，不是只有空数据库。
- 源码版可以导入截图，尝试定位物品，识别名称和数量，展示候选图与本地参考价，由用户逐项核对。
- 已有按每格参考价值从低到高排列的逻辑；缺价、未确认、隐藏枪械详情等情况下必须谨慎处理，不能直接指导丢弃。
- 已有演示用装箱优化器和操作步骤原型；真实截图流程目前只做识别、核对与估价，尚未接入完整最优理包。
- 桌面 `DeltaLootAssistant` 中的 EXE 是旧构建。修改源码不会自动更新它，不能用它判断最新源码功能是否完成。

### 二、三个位置分别是什么

| 位置 | 用途 | 你通常怎么使用 |
|---|---|---|
| `C:\Users\ASUS\Desktop\delta force\delta-loot-assistant` | 真正的源代码、测试、开发环境、构建配置 | 从这里启动最新源码、查看和修改项目 |
| `C:\Users\ASUS\Desktop\DeltaLootAssistant` | 已安装的旧 EXE 和随附依赖 | 保留即可；重新打包验证前不作为最新版本入口 |
| `C:\Users\ASUS\AppData\Local\DeltaLootAssistant` | 程序真实运行数据 | 目录、价格库、图标、校准配置、加密凭证及报告在这里 |

`AppData` 是 Windows 默认隐藏目录。按 `Win+R`，输入 `%LOCALAPPDATA%\DeltaLootAssistant` 即可打开。它不在两个桌面目录里面，但不解释它，就无法解释数据库的来源和位置。

源码入口：[双击 start.cmd](<C:/Users/ASUS/Desktop/delta force/delta-loot-assistant/start.cmd>)。脚本用当前项目 `.venv` 内的 Python 启动应用，不会调用桌面旧 EXE。

### 三、已完成工作，以及每项工作的原理

#### 1. 真实物品目录和价格快照

已实现授权数据源客户端、目录解析、价格关联和 SQLite 保存。当前本机数据库有 **1 个成功目录快照、1,722 条物品定义、1,353 条已关联且大于零的物品价格**。这不等于全部物品都有价格，也不等于每条记录都是不同的游戏物品；供应商会有变体记录。

原理是分两类数据处理：`item_info_all` 提供名字、编号、尺寸、品质、图标地址；`item_price_all` 提供价格。用基础目录的 `oid` 对应价格记录的 `id`，转换成本项目的物品 ID，再一起存入本地数据库。Token 只是调用接口的通行证，不包含识别能力。

当前快照元信息：区域 `cn`、赛季标记 `live-cn`；价格包 `generated_at` 为 `2026-09-07T19:41:36+00:00`，本地同步记录 `synced_at` 为 `2026-09-08T05:14:06.729330+00:00`。这两个时间都是 UTC。包时间不保证每一个物品的报价都同样新鲜，未来还需逐物品时间与来源追踪。

后续估价只查本地库，不需要每次识别都使用 Key。“24 小时保护”是禁止过早重复手动同步，不是自动每日更新。当前任务没有请求付费接口。一次完整手动同步涉及目录和价格两个端点，不应误解为供应商只计一次调用。

#### 2. 本地真实图标库

**有具体 PNG 图像，不是只有名字。** 接口目录中的 `pic` 字段给出了图标地址；程序将这些图片缓存到本地。当前 `icon-index.json` 有 **1,720 个物品 ID 映射，指向 1,510 张不同 PNG**。多个记录共用图片是正常情况，所以“图标相同”不能单独证明物品型号相同。

原理：对图标 URL 做 SHA-256，作为缓存文件名；索引记录“本地物品 ID → PNG 路径”。这样不同物品引用同一 URL 时只需保存一张。这里是接口提供的目录图片，不是从你的截图训练出的模型。图标地址通常来自 `playerhub.df.qq.com`；不能由此推断获得了再分发这些素材的授权。

#### 3. 截图怎样对应到价格中的物品

实际关联链是：

```text
截图中的物品区域
  → OCR 读出名称/数量 + 图标特征比对 + 格子位置
  → 在本地目录中选出候选物品 ID
  → 用户核对并确认候选、数量和必要的价格修正
  → 用同一个物品 ID 查询 SQLite 中的价格包
  → 单价 × 数量（必要时应用暂定耐久规则）
```

最关键的是中间的“物品 ID”。目录的名字、图标和价格记录已在导入时建立关联，不是看到一个像枪的图案就随便拿一个枪价，也不是把截图发送给付费接口识别。

名称识别会先清理空格、标点和部分 OCR 混淆字符，再做名称/别名的模糊匹配。图标匹配结合 ORB 局部特征、颜色直方图和边缘信息提供辅助证据。格子位置和尺寸用于确定物品区域；这些证据仍可能出错，所以当前真实截图候选均需人工确认。程序显示的匹配分数不是经过统计验证的“正确概率”。

#### 4. 本地 OCR 与截图区域识别

已接入 RapidOCR 和 ONNX Runtime，在本机把像素变成文字。检测模型找文字框，方向模型辅助判断文字方向，识别模型把文字区域变成字符。模型在 `.venv\Lib\site-packages\rapidocr\models` 下：

| 文件 | 用途 |
|---|---|
| `PP-OCRv6_det_small.onnx` | 文字区域检测 |
| `ch_ppocr_mobile_v2.0_cls_mobile.onnx` | 文字方向分类 |
| `PP-OCRv6_rec_small.onnx` | 文字内容识别 |

它们是通用 OCR 模型，不是三角洲全物品图像库，也不包含物价。正常识别使用本地模型；缺少模型会提示组件不完整，不依赖临时在线识别。

当前校准主要针对你给出的 1920×1080 截图：背包实际储物区、右侧散落物区、安全箱储物区。背包约为 `(882,222)-(1207,677)`、5 列 7 行；安全箱约为 `(882,762)-(1077,957)`、3 列 3 行；散落物约为 `(1291,147)-(1616,927)`、5 列 12 行，格距约 65 像素。这些是样图布局，不是所有背包的通用坐标。

程序先识别区域文字，再按格子位置组织候选，并对细小数量或合并标签补充局部 OCR。**目前没有完整覆盖口袋、胸挂、左侧装备栏及隐藏武器配件。** 换 UI 布局、背包或分辨率后可能需要重新校准。区域矩形校准界面也还没有完整联动调整格距与行列数。

#### 5. 人工纠错与多截图会话

已实现候选图片预览、搜索目录、修改物品名/数量/耐久/价格、锁定和确认。确认会更新已有候选实例，避免确认一次就重复加入一个物品。未知项会保留来源和数量，不能静默当成已识别。

多截图通过完整帧哈希避免重复处理相同帧；通过物品位置、指纹和重叠锚点尝试合并滚动区域，滚动估计需要至少两个可用的独特锚点。它还是初步合并机制，不能保证相同物品、遮挡、动画或大幅滚动下完全不漏不重。隐藏整枪详情补拍关联尚未完整实现。

默认不保存整张截图。开启贡献样本后，才保存确认过的局部裁剪和标签；这为以后改进识别提供材料，不是已经实现了自动训练和发布模型。

#### 6. 估价与最低价值排序

已实现本地价格提供器、版本化 JSON/CSV 导入、价格回滚、数量估价和单格价值排序。实际建议偏向“每占一格价值最低”的可替换物品，并同时展示整件总价。最低总价和最低单格价值是两种不同口径，不能混为一谈。

例如本次本地快照中，若确认是 60 发 M80、每发 1,498，则总参考价是 `60 × 1,498 = 89,880`；不是把整堆按一发价格算。数量 OCR 读错或默认成 1，就会直接影响排名，必须核对。

未定价物品会在界面标出；底层估值函数对缺价返回 0，所以总览只能叫“已识别参考值”，不能当作完整背包真实总价。推荐逻辑会检查未决识别、未确认详情和缺价，且跳过锁定项和已经装在父物品上的子项。

父子装配关系手动建好后，估价可以累计枪体、配件、弹匣和弹药，并避免把已计入的子项再次独立汇总。但截图还不能可靠读取完整枪械配置。比如 M7 的目录参考价只代表目录记录，不代表截图里满改枪的整套价值。不要仅凭这个数字丢枪。耐久目前也是简单线性修正，并未验证真实交易规则。

#### 7. 桌面入口、热键和隐私边界

已有独立 Qt 窗口、F8/F9 全局热键注册、导入本地截图及屏幕捕获。截图使用 mss，根据前台窗口中心位置选取显示器；检查显示器是否为 1920×1080，但并没有真正验证游戏语言、UI 缩放或前台是否确实为游戏。

F8 追加截图，F9 完成核对后的估价流程。Esc 当前是 Qt 窗口内的动作，不是已注册的全局热键。注册热键只是接收用户按键，不会向游戏发送操作。真实截图流程不会调用完整优化器。

Token 使用当前 Windows 用户的 DPAPI 加密，文件搬到另一用户或电脑不保证可解密。不要把加密凭证、真实数据库或截图上传 Git。已经在聊天中发过的 Key 建议在联调结束后轮换，不需要再次在聊天里贴出来。

#### 8. 优化器与操作步骤：已完成的是原型

已有 CP-SAT 原型：枚举物品合法放置位置，为候选位置设布尔变量，加上不重叠、锁定、禁用格、安全箱、堆叠、部分配件/弹匣/装备槽、条件容器约束。通过分阶段目标尝试优先价值，再安全箱保护价值、操作数和负重。

已有步骤生成原型，根据当前位置和目标位置列出丢弃、拿取、移动、拆装、合并等，并对部分循环交换给出临时空间提示。但还没有完整验证每一步操作之后的容器容量、临时格尺寸和所有枪械兼容规则。

因此目前不能把演示 `OPTIMAL` 理解为真实游戏最高收益。真实截图尚未接入完整模型，模型规则也需进一步校验。配置虽设为 5 秒，建模时间和分阶段回退尚未统一纳入严格总截止时间，不能承诺绝对不超过 5 秒。

#### 9. 验证与打包状态

最近一次检查：37 个自动测试通过，`ruff check src tests` 通过，`pip check` 未发现依赖冲突。测试多数是合成物品、假 OCR、假客户端及小规模约束用例，不是完整真实对局验收。随机暴力对照目前仅覆盖 12 个简单的一格物品小实例，不能替代一般二维装箱的全面验证。

你的样图曾得到 39 个候选、39 个待核对项；不是“39 个都正确”。有相近弹药型号混淆等已知问题。一次本机记录为初始化约 8.04 秒、识别约 7.08 秒，不满足此前通常 2 秒的目标；也没有经过 200–500 张标注集验证识别准确率。当前识别仍可能阻塞应用窗口。

已有 PyInstaller 配置、Inno Setup 脚本和旧安装产物。桌面 EXE 与源码 `dist` 中 EXE 的最后写入时间是 2026-07-23；旧安装目录未见 RapidOCR 目录。新的 OCR 收集配置虽然写进了打包配置，但本轮没有重新构建、安装或验收新包。

### 四、每个文件和文件夹的作用

两个桌面目录合计约四万个文件，大多数来自两个 Python 虚拟环境、Qt 和构建产物。下面逐一说明项目自有文件；**每一个实际文件的路径与用途**另见 [完整逐文件清单索引](<C:/Users/ASUS/Desktop/delta force/delta-loot-assistant/docs/file-inventory/INDEX.md>)。清单也额外覆盖真实运行数据目录。

项目业务文件的说明来自源码检查；第三方库文件按所属库和扩展名归类，不宣称逐行审计了四万个文件。目录没有扩展名的资源、许可证、二进制、缓存也会列入清单。清单自身不递归收录，避免不断生成新的清单条目。

#### A. 源码目录顶层

以下路径均相对于 `C:\Users\ASUS\Desktop\delta force\delta-loot-assistant`。

| 文件或文件夹 | 作用 / 是否需要你编辑 |
|---|---|
| `AGENTS.md` | 本交接手册与后续开发约束；功能改变后同步更新 |
| `README.md` | 安装、启动和当前功能限制说明 |
| `.gitignore` | 防止环境、缓存、数据库及敏感运行数据被 Git 收录 |
| `pyproject.toml` | 项目名称、Python 依赖、运行入口及测试/静态检查配置 |
| `start.cmd` | 最新源码的双击启动入口 |
| `build.ps1` | 安装构建依赖、检查、调用打包工具；不是普通使用入口 |
| `smoke.sqlite3` | 旧冒烟运行用数据库，不是当前真实价格库；本次未删除 |
| `.git/` | Git 仓库元信息；目前尚未形成已提交的版本历史，不等于已经备份 |
| `.venv/` | 当前运行和测试使用的 Python 环境；里面的库不是手写业务文件 |
| `.venv-test/` | 旧测试环境；`start.cmd` 不使用它 |
| `.pytest_cache/` | 测试缓存，不是用户数据 |
| `.ruff_cache/` | 静态检查缓存 |
| `src/` | 应用源代码，详见下表 |
| `tests/` | 自动测试，详见下表 |
| `resources/` | 可随源码发布的演示配置、演示目录与演示价格 |
| `packaging/` | EXE 与安装程序的构建配置 |
| `build/` | PyInstaller 自动生成的临时归档、依赖清单、警告和交叉引用报告 |
| `dist/DeltaLootAssistant/` | 上次打包的 EXE 与运行依赖副本，不自动跟随源码 |
| `dist/installer/` | Inno Setup 输出的旧安装包 |
| `tools/describe_file_inventory.py` | 本次新增的只读清单工具，列举路径、分类用途，向标准输出输出清单，不读取凭证内容 |
| `docs/file-inventory/INDEX.md` | 全量文件清单总入口 |
| `docs/file-inventory/development-*.md` | 源码外层目录的逐文件分页清单，每页最多 500 项 |
| `docs/file-inventory/installed-*.md` | 旧安装目录的逐文件分页清单 |
| `docs/file-inventory/data-*.md` | AppData 运行数据的逐文件分页清单 |

外层 `C:\Users\ASUS\Desktop\delta force\AGENTS.md` 是指向本手册的入口，`delta force` 本身不是另一套程序。

#### B. 应用源代码

下面均位于 `src/delta_loot_assistant/`，不是桌面安装目录。

| 文件 | 具体作用 |
|---|---|
| `__init__.py` | 标记 Python 包，导出公共模型与版本号 |
| `__main__.py` | 支持 `python -m delta_loot_assistant` 启动，转入应用入口 |
| `app.py` | 创建 QApplication、控制器、窗口与事件循环，管理热键生命周期 |
| `models.py` | 定义物品、实例、容器、位置、价格包、扫描状态与结果等数据结构 |
| `paths.py` | 决定开发/打包资源路径以及 AppData 数据库、凭证、模板目录 |
| `capture.py` | 捕获活动显示器，校验目标分辨率 |
| `hotkeys.py` | 封装 Windows RegisterHotKey 的 F8/F9 接收与注销 |
| `secrets.py` | 当前 Windows 用户 DPAPI 加密凭证的保存和读取 |
| `orzice.py` | 付费接口客户端、目录解析、供应商 ID 关联、目录快照与重复同步保护 |
| `catalog.py` | 加载定义、搜索名称/别名、文本归一化和模糊匹配 |
| `icon_cache.py` | 图标 URL 缓存、共享图片去重和 ID 到 PNG 的索引 |
| `pricing.py` | 本地价格提供器、JSON/CSV 导入、SQLite 价格版本与回滚、数量/耐久/装配估价 |
| `recognition.py` | 区域配置、格子组织、RapidOCR、局部数量识别、图标匹配和待确认候选 |
| `scan_session.py` | 保存扫描会话、重复帧检查、重叠合并、未决项确认和完整性状态 |
| `value_ranking.py` | 整件/每格参考价值计算，排序和最低可替换项的安全阻断 |
| `controller.py` | 将截图、识别、纠错、价格与 UI 信号连接起来，区分真实估价和演示求解 |
| `optimizer.py` | 候选放置枚举、CP-SAT 约束与分级目标原型 |
| `action_planner.py` | 从现状到目标布局生成步骤的原型，包含粗略循环/临时空间提示 |
| `screenshot_report.py` | 命令行离线读取截图和本地价格，生成候选估价 Markdown 报告 |
| `ui/__init__.py` | 标记 UI 子包 |
| `ui/main_window.py` | 主窗口、导入截图、物品表格、候选预览与人工编辑、价格操作及结果展示 |
| `ui/calibration.py` | 截图预览和区域坐标编辑界面 |
| `ui/layout_preview.py` | 绘制演示目标容器的格子及物品布局 |
| 各层 `__pycache__/*.pyc` | Python 执行生成的字节码缓存；不需要手动修改 |

#### C. 配置与打包文件

| 文件 | 具体作用 |
|---|---|
| `resources/app_config.json` | 热键、分辨率、识别阈值、求解时间等默认配置 |
| `resources/sample_catalog.json` | 演示物品/容器定义，不是 1,722 条真实目录 |
| `resources/sample_price_pack.json` | 演示价格，不是付费获得的真实快照 |
| `packaging/entrypoint.py` | PyInstaller 使用的启动入口 |
| `packaging/delta_loot_assistant.spec` | 指定打包脚本、资源和依赖收集；包含新 OCR 的收集配置 |
| `packaging/installer.iss` | 指定 Inno Setup 安装文件、快捷方式、卸载和输出位置 |

#### D. 测试文件

| 文件 | 检查内容 / 覆盖边界 |
|---|---|
| `tests/conftest.py` | 共用的虚拟目录、容器和价格夹具 |
| `tests/test_action_planner.py` | 交换循环/临时空间提示，不是完整操作执行模拟 |
| `tests/test_catalog.py` | 名称匹配、数字文本过滤 |
| `tests/test_controller.py` | 装配关系与确认后样本裁剪等控制器流程 |
| `tests/test_icon_cache.py` | 假下载器下共享图标只下载一次、索引构建 |
| `tests/test_optimizer.py` | 装箱、锁定、隔断、堆叠、配件、容器替换及简单暴力对照 |
| `tests/test_orzice.py` | 假客户端下快照保存、字段/ID/尺寸映射、同步间隔保护 |
| `tests/test_pricing.py` | 导入、版本回滚、数量、耐久、父子物品去重计价 |
| `tests/test_recognition.py` | 合成图和假 OCR 下校准、数量、候选与确认，不是大规模准确率验收 |
| `tests/test_scan_session.py` | 同帧去重、重复物品的位置区分、滚动重叠、原位确认 |
| `tests/test_value_ranking.py` | 单格排序、锁定、缺价阻断和装配子物品处理 |

#### E. 桌面安装目录：DeltaLootAssistant

| 文件或目录 | 具体作用 |
|---|---|
| `AGENTS.md` | 本次增加的安装目录说明入口，指向源码交接手册 |
| `DeltaLootAssistant.exe` | 旧版 PyInstaller 启动程序；最后写入日期 2026-07-23 |
| `unins000.exe` | Inno Setup 卸载器，运行会启动卸载；本次没有运行 |
| `unins000.dat` | 安装/卸载文件记录，不是游戏物品数据 |
| `_internal/` | EXE 运行需要的 Python、Qt、图像和求解依赖，不能随便移动或删减 |
| `_internal/resources/` | 旧包附带的演示配置/目录/价格，非 AppData 真实数据 |
| `_internal/PySide6/`、`shiboken6/` | Qt 窗口、平台插件和 Python/C++ 绑定 |
| `_internal/cv2/`、`PIL/` | OpenCV 与 Pillow 图像处理支持 |
| `_internal/onnxruntime/` | 本地神经网络推理运行库；只有运行库不等于已经带齐 OCR 模型 |
| `_internal/ortools/`、`google/` | 约束优化及 Protocol Buffers 等支持 |
| `_internal/numpy/`、`numpy.libs/` | 数组处理及原生数学库 |
| `_internal/pandas/`、`pandas.libs/` | 表格数据支持及原生依赖 |
| `_internal/dateutil/`、`tzdata/` | 日期解析和时区数据 |
| `_internal/setuptools/`、`pyreadline3-3.5.6.dist-info/` | 包运行支持与安装元数据 |
| 其他 `.dll`、`.pyd`、`.zip` | Python/系统动态库、原生扩展和标准库归档，逐项路径见完整清单 |

`.venv` 内的 `.dist-info/METADATA` 是依赖版本/要求，`RECORD` 是安装文件清单，`WHEEL` 是分发格式说明，许可证文件是第三方授权文本；它们都不是物品价格表。Qt 的 `.qm` 是翻译资源，`.qml` 是界面组件，`.qsb` 是着色器。清单会保留每个此类文件的实际路径。

#### F. 真正的数据库、图片和识别报告

以下相对于 `C:\Users\ASUS\AppData\Local\DeltaLootAssistant`：

| 路径 | 具体作用 |
|---|---|
| `assistant.sqlite3` | 当前真实 SQLite 数据库，约 1.8 MB；不是图片训练模型 |
| `layout_1080p.json` | 本机截图储物区、格距、行列等校准配置 |
| `orzice-token.dpapi` | Windows 加密凭证；不要打开分享，不要上传仓库 |
| `templates/icon-index.json` | 物品 ID 与本地 PNG 的关联表 |
| `templates/provider-icons/*.png` | 实际缓存物品图标，每张文件在完整清单里独立列出 |
| `reports/screenshot-review.md` | 样图的离线候选和参考价格报告，不是已经确认的丢弃清单 |
| 贡献样本目录（启用后才产生） | 已确认的局部物品裁剪和标签；当前不应假定已有完整训练集 |

SQLite 内主要有三张业务表：

| 表 | 保存什么 |
|---|---|
| `catalog_snapshots` | 目录版本、同步时间与来源等快照信息 |
| `catalog_items` | 目录记录、解析后的定义、原始 JSON 和图标 URL；PNG 像素不在表里 |
| `price_packs` | 版本化价格包及物品 ID 到价格的 JSON 映射 |

要观察数据，不需要再联网。可以使用 SQLite 查看工具以只读方式打开数据库，或在应用中搜索物品并查看本地价。不要为查看数据双击“同步”。如果正在写入数据库，备份应使用 SQLite 备份机制或先关闭应用，不要随意只复制部分正在写入的数据库文件。

### 五、未来要做什么：按先后顺序

以下是未完成工作，不是要求你全部亲自编程。

| 优先级 | 开发者下一步工作 | 完成判据 |
|---|---|---|
| P0：可信估价 | 把未知数量、未确定弹药型号、缺价、整枪详情分别显式标记；严格阻止未经确认的最低价推荐；明确总价/单格价切换 | 可逐项解释名称、ID、数量、价源；不把未知当便宜物品 |
| P0：识别覆盖 | 校准格距和不同容器布局，补口袋/胸挂/装备栏；改进相似弹药和旋转图标；建立人工标注回归集 | 用户样图无静默遗漏；错误可纠正且不重复入账 |
| P1：数据一致性 | 将目录和价格作为关联版本原子保存；记录失败付费尝试避免误重试；完善逐条报价时间、来源、手动价格覆盖 | 失败不留下混合版本，不发生未授权补请求 |
| P1：游戏规则 | 核实尺寸、禁止旋转、堆叠、耐久/剩余使用量、容器限制、枪械配件和装弹规则 | 每条规则有来源/验证状态，无法确认的规则不冒充已支持 |
| P1：整枪估价 | 详情补拍关联，解析配件/弹匣/弹药；区分整枪人工总价与分件价 | 子物品恰好计价一次，整枪未知配置不会被按裸枪推荐丢弃 |
| P2：性能与交互 | OCR 放后台工作线程，缓存特征与结果，取消/超时、错误提示、热键冲突处理 | 界面不冻结；用固定设备和截图集报告耗时而非口头承诺 |
| P3：完整优化 | 补齐条件容器/枪械变换约束、真实扫描到优化器映射、严格总截止时间和状态处理 | 小规模一般二维用例与暴力对照；超时明确仅当前最佳或无方案 |
| P3：可执行步骤 | 建立逐步状态模拟器，检查临时空间、循环交换、容器替换、装拆顺序 | 每一步在前一步结果上合法，不能只给最终摆法 |
| P4：发行验收 | 重新打包并检查 OCR 资源；构建脚本严格检查各工具退出码；Windows 11 安装/双屏/热键/卸载测试 | 源码与新 EXE 功能一致，运行不请求未授权网络 |

原计划的 99% 容器检测、93% Top-1、98% Top-3、常见 2 秒等都是待验证指标，不能写成已经达成的简历成绩。真实截图足够、规则验证和性能测试完成后，才能扩大产品承诺。

### 六、你现在需要做什么

先把“这张图估得可信”完成，不需要你先手工录入 1,722 个物品，也不需要再次购买 Key。

1. **启动源码版**：双击 `C:\Users\ASUS\Desktop\delta force\delta-loot-assistant\start.cmd`。暂时不要用桌面旧 EXE 验证最新功能。
2. **先不更新物价**：已有快照能用于验证识别流程。过期价格只影响参考价值的新鲜程度，不妨碍检查名称、数量和 ID 的对应关系。确实需要新价格时，再由你明确手动触发，并接受可能产生的接口费用。
3. **导入现有截图**：你已经提供了一张，不必再发同一张。可以先用“导入截图”检查，不必同时打开游戏。以后若截图存在本机，只要给出文件路径，就能继续离线处理，不要求你上传给第三方接口。
4. **核对候选**：检查物品名、弹药型号、数量及裁剪位置。认不准的保留未知；枪械没有配件详情时保留待确认，或者输入你确认过的整枪参考价。不要为了让界面显示结果随意点确认。
5. **告诉开发者错误**：用“截图位置 / 实际名字 / 实际数量 / 程序显示什么”描述。你提供正确答案，开发者把它变成回归测试、别名或模板修正；你不需要手写数据库 ID。
6. **有条件再补样图**：先补 5–10 张未经缩放的原始 1080p 截图，优先不同背包、滚动前后、容易混淆的弹药和枪械详情。不是现在就要求你凑齐 200–500 张；那个规模是以后建立可靠准确率验收集用的。
7. **验收顺序**：先验证所有可见普通物品能核对估价和排序，再做整枪/全装备覆盖，最后做替换和最高收益布局。当前不应把未经核对的排名直接用于丢贵重物品。

如果源码窗口不能启动，先提供错误文字或本地日志位置；不要为修复启动问题重新请求付费接口。若同屏有个人信息，可先使用离线方式处理，并在共享样本前确认隐私范围。

### 七、以后接手本项目的代理必须知道

- 第一阶段真实需求是截图逐物品估价和低价值排序；不要绕过基础可信度，直接宣称实现全局最高收益。
- 本手册中的数量和日期是 2026-09-08 的本机检查快照，不是永远不变的常量。若变更代码/数据，应同步更新对应状态与证据。
- 不要因为看到安装目录就在那里直接改 DLL，也不要把源码运行数据错误归到 `resources/sample_*`。
- 本次未修改游戏或业务逻辑、未删除文件、未调用付费接口、未构建或安装新 EXE。新增的是说明入口、此手册、只读清单辅助脚本和清单文件。
- 逐文件清单是静态盘点。后续安装依赖、运行测试或新增文件后会变化；更新时使用只读列举工具，文档写入仍按本仓库 `apply_patch` 规则执行，严禁读取并抄入凭证内容。
